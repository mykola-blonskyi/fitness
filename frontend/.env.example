# Hub base URL (no trailing slash) — see my-projects/boilerplates/subdomain-app.md
API_URL=https://blonskyi.dev

# This app's own public origin (no trailing slash) — used to build absolute
# URLs (e.g. the login callbackUrl) instead of trusting request.url, which
# resolves to the container's internal bind address behind Coolify/Traefik.
APP_URL=http://localhost:3000

# Must match the Hub's AUTH_SECRET exactly — JWT verification fails otherwise
AUTH_SECRET=

# Shared cookie domain — must be .blonskyi.dev (dot-prefixed) for cross-subdomain sharing
COOKIE_DOMAIN=.blonskyi.dev

# The project slug registered in the Hub database
PROJECT_SLUG=fitness

# Internal-only backend endpoint (ADR-001) — Docker service DNS name in
# prod (http://backend:3001), localhost in local dev
BACKEND_URL=http://localhost:3001

# Local dev only — the .blonskyi.dev cookie domain doesn't resolve on
# localhost. Set DEV_BYPASS_AUTH=true and fill in a fake identity to
# exercise the app locally without a real Hub. Never honored in production
# regardless of what this file contains — proxy.ts hard-gates on NODE_ENV.
DEV_BYPASS_AUTH=false
DEV_USER_ID=
DEV_USER_EMAIL=

# Sentry error tracking (see docs/decisions.md ADR-006) — NEXT_PUBLIC_
# prefix is required even though this app has no other public runtime
# config, since Next.js inlines it into the browser bundle at build time.
# Only active when NODE_ENV=production, so this can stay unset locally.
NEXT_PUBLIC_SENTRY_DSN=

# Build-only (source map upload) — never needed at container runtime.
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
